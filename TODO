BUGS
====

- rmdir: Handle pagination for clear_container


FEATURES
========


- Named-endpoints
    - bf_load_endpoint
    - bf_load_endpoint_file
    - bf_load_endpoint_interactive
        - cf_load_endpoint_interactive
        - s3_load_endpoint_interactive

    - bf_save_endpoint
        - cf_save_endpoint
        - s3_save_endpoint

    - Make credential request driver-specific
    - Move to new directory structure

# Use default endpoint
bashfiles get container foo

equivalent to

bashfiles -e default get container foo

# Use 'bar' endpoint
bashfiles -e bar get container foo

OPTION 2a

- Source general config
- Source endpoint config

~/.bashfiles/
    config
    endpoints/
        default
            CF_USERNAME=myuser
        s3
            S3_USERNAME=myuser
    completion/
        default/
            container-names
            <container>-object-names


CRUD:

bashfiles endpoints ls
bashfiles endpoints add bar
bashfiles endpoints rm bar





- Make container public

- Switch to SLO large-files?

- Add context-help:
    - bashfiles help (general help)
    - bashfiles help get (get specific help)

- Add Cloudfiles v2 Auth

- Add CHANGELOG


TEST
====

- Test against Linux

- Test large-file upload
